function marshallBooleanType(v) {}
function marshallNumberType(v) {}
function marshallStringType(v) {}
const NodeObject = function() {};
//import assert from 'assert';
function assert(mustBeTruthy) {}
function hop(value, key) {
  return Object.prototype.hasOwnProperty.call(value, key);
}
export { marshallSourceFile };
window.marshallSourceFile = marshallSourceFile;
// exports.marshallSourceFile = marshallSourceFile;
function marshallSyntaxKind_AbstractKeyword(value) {}
function marshallSyntaxKind_AsyncKeyword(value) {}
function marshallSyntaxKind_ConstKeyword(value) {}
function marshallSyntaxKind_DeclareKeyword(value) {}
function marshallSyntaxKind_DefaultKeyword(value) {}
function marshallSyntaxKind_ExportKeyword(value) {}
function marshallSyntaxKind_PublicKeyword(value) {}
function marshallSyntaxKind_PrivateKeyword(value) {}
function marshallSyntaxKind_ProtectedKeyword(value) {}
function marshallSyntaxKind_ReadonlyKeyword(value) {}
function marshallSyntaxKind_StaticKeyword(value) {}
function marshallSyntaxKind_Decorator(value) {}
function marshallSyntaxKind_Identifier(value) {}
function marshallSyntaxKind_StringLiteral(value) {}
function marshallSyntaxKind_NumericLiteral(value) {}
function marshallSyntaxKind_NoSubstitutionTemplateLiteral(value) {}
function marshallSyntaxKind_ComputedPropertyName(value) {}
function marshallSyntaxKind_PrivateIdentifier(value) {}
function marshallSyntaxKind_ElementAccessExpression(value) {}
function marshallSyntaxKind_QuestionDotToken(value) {}
function marshallSyntaxKind_ObjectBindingPattern(value) {}
function marshallSyntaxKind_Parameter(value) {}
function marshallSyntaxKind_CallSignature(value) {}
function marshallSyntaxKind_JSDocComment(value) {}
function marshallSyntaxKind_ConstructSignature(value) {}
function marshallSyntaxKind_JSDocTypeLiteral(value) {}
function marshallSyntaxKind_QualifiedName(value) {}
function marshallSyntaxKind_JSDocTypeExpression(value) {}
function marshallSyntaxKind_QuestionToken(value) {}
function marshallSyntaxKind_MethodSignature(value) {}
function marshallSyntaxKind_ClassDeclaration(value) {}
function marshallSyntaxKind_ClassExpression(value) {}
function marshallSyntaxKind_InterfaceDeclaration(value) {}
function marshallSyntaxKind_TypeLiteral(value) {}
function marshallSyntaxKind_PropertySignature(value) {}
function marshallSyntaxKind_ArrowFunction(value) {}
function marshallSyntaxKind_EqualsGreaterThanToken(value) {}
function marshallSyntaxKind_Block(value) {}
function marshallSyntaxKind_AsteriskToken(value) {}
function marshallSyntaxKind_ExclamationToken(value) {}
function marshallSyntaxKind_ParenthesizedExpression(value) {}
function marshallSyntaxKind_ObjectLiteralExpression(value) {}
function marshallSyntaxKind_SpreadAssignment(value) {}
function marshallSyntaxKind_ShorthandPropertyAssignment(value) {}
function marshallSyntaxKind_EqualsToken(value) {}
function marshallSyntaxKind_PropertyAssignment(value) {}
function marshallSyntaxKind_FunctionExpression(value) {}
function marshallSyntaxKind_LabeledStatement(value) {}
function marshallSyntaxKind_ExpressionStatement(value) {}
function marshallSyntaxKind_VariableStatement(value) {}
function marshallSyntaxKind_VariableDeclarationList(value) {}
function marshallSyntaxKind_ForStatement(value) {}
function marshallSyntaxKind_ForInStatement(value) {}
function marshallSyntaxKind_ForOfStatement(value) {}
function marshallSyntaxKind_AwaitKeyword(value) {}
function marshallSyntaxKind_FunctionDeclaration(value) {}
function marshallSyntaxKind_Constructor(value) {}
function marshallSyntaxKind_MethodDeclaration(value) {}
function marshallSyntaxKind_PropertyDeclaration(value) {}
function marshallSyntaxKind_GetAccessor(value) {}
function marshallSyntaxKind_SetAccessor(value) {}
function marshallSyntaxKind_TypeAliasDeclaration(value) {}
function marshallSyntaxKind_EnumMember(value) {}
function marshallSyntaxKind_EnumDeclaration(value) {}
function marshallSyntaxKind_ModuleDeclaration(value) {}
function marshallSyntaxKind_SourceFile(value) {}
function marshallSyntaxKind_EndOfFileToken(value) {}
function marshallSyntaxKind_ModuleBlock(value) {}
function marshallSyntaxKind_ImportEqualsDeclaration(value) {}
function marshallSyntaxKind_ExternalModuleReference(value) {}
function marshallSyntaxKind_IndexSignature(value) {}
function marshallSyntaxKind_FunctionType(value) {}
function marshallSyntaxKind_ConstructorType(value) {}
function marshallSyntaxKind_JSDocFunctionType(value) {}
function marshallSyntaxKind_ExportDeclaration(value) {}
function marshallSyntaxKind_NamespaceExport(value) {}
function marshallSyntaxKind_NamedExports(value) {}
function marshallSyntaxKind_DotDotDotToken(value) {}
function marshallSyntaxKind_ArrayBindingPattern(value) {}
function marshallSyntaxKind_VariableDeclaration(value) {}
function marshallSyntaxKind_CatchClause(value) {}
function marshallSyntaxKind_TryStatement(value) {}
function marshallSyntaxKind_BindingElement(value) {}
function marshallSyntaxKind_PropertyAccessExpression(value) {}
function marshallSyntaxKind_TypeParameter(value) {}
function marshallSyntaxKind_InferType(value) {}
function marshallSyntaxKind_JSDocTemplateTag(value) {}
function marshallSyntaxKind_HeritageClause(value) {}
function marshallSyntaxKind_ExpressionWithTypeArguments(value) {}
function marshallSyntaxKind_JSDocAugmentsTag(value) {}
function marshallSyntaxKind_OmittedExpression(value) {}
function marshallSyntaxKind_ExportSpecifier(value) {}
function marshallSyntaxKind_PartiallyEmittedExpression(value) {}
function marshallSyntaxKind_PrefixUnaryExpression(value) {}
function marshallSyntaxKind_PostfixUnaryExpression(value) {}
function marshallSyntaxKind_NullKeyword(value) {}
function marshallSyntaxKind_ThisKeyword(value) {}
function marshallSyntaxKind_SuperKeyword(value) {}
function marshallSyntaxKind_ImportKeyword(value) {}
function marshallSyntaxKind_DeleteExpression(value) {}
function marshallSyntaxKind_TypeOfExpression(value) {}
function marshallSyntaxKind_VoidExpression(value) {}
function marshallSyntaxKind_AwaitExpression(value) {}
function marshallSyntaxKind_YieldExpression(value) {}
function marshallSyntaxKind_SyntheticExpression(value) {}
function marshallSyntaxKind_AsteriskAsteriskToken(value) {}
function marshallSyntaxKind_BinaryExpression(value) {}
function marshallSyntaxKind_ArrayLiteralExpression(value) {}
function marshallSyntaxKind_SpreadElement(value) {}
function marshallSyntaxKind_CallExpression(value) {}
function marshallSyntaxKind_NewExpression(value) {}
function marshallSyntaxKind_ConditionalExpression(value) {}
function marshallSyntaxKind_ColonToken(value) {}
function marshallSyntaxKind_RegularExpressionLiteral(value) {}
function marshallSyntaxKind_BigIntLiteral(value) {}
function marshallSyntaxKind_TemplateHead(value) {}
function marshallSyntaxKind_TemplateExpression(value) {}
function marshallSyntaxKind_TemplateSpan(value) {}
function marshallSyntaxKind_TemplateMiddle(value) {}
function marshallSyntaxKind_TemplateTail(value) {}
function marshallSyntaxKind_TaggedTemplateExpression(value) {}
function marshallSyntaxKind_JsxOpeningElement(value) {}
function marshallSyntaxKind_JsxElement(value) {}
function marshallSyntaxKind_JsxClosingElement(value) {}
function marshallSyntaxKind_JsxAttributes(value) {}
function marshallSyntaxKind_JsxSelfClosingElement(value) {}
function marshallSyntaxKind_JsxAttribute(value) {}
function marshallSyntaxKind_JsxExpression(value) {}
function marshallSyntaxKind_JsxSpreadAttribute(value) {}
function marshallSyntaxKind_JsxFragment(value) {}
function marshallSyntaxKind_JsxOpeningFragment(value) {}
function marshallSyntaxKind_JsxClosingFragment(value) {}
function marshallSyntaxKind_JsxText(value) {}
function marshallSyntaxKind_AsExpression(value) {}
function marshallSyntaxKind_TypeAssertionExpression(value) {}
function marshallSyntaxKind_NonNullExpression(value) {}
function marshallSyntaxKind_MetaProperty(value) {}
function marshallSyntaxKind_NotEmittedStatement(value) {}
function marshallSyntaxKind_EndOfDeclarationMarker(value) {}
function marshallSyntaxKind_CommaListExpression(value) {}
function marshallSyntaxKind_MergeDeclarationMarker(value) {}
function marshallSyntaxKind_SyntheticReferenceExpression(value) {}
function marshallSyntaxKind_EmptyStatement(value) {}
function marshallSyntaxKind_DebuggerStatement(value) {}
function marshallSyntaxKind_MissingDeclaration(value) {}
function marshallSyntaxKind_CaseClause(value) {}
function marshallSyntaxKind_CaseBlock(value) {}
function marshallSyntaxKind_SwitchStatement(value) {}
function marshallSyntaxKind_DefaultClause(value) {}
function marshallSyntaxKind_IfStatement(value) {}
function marshallSyntaxKind_DoStatement(value) {}
function marshallSyntaxKind_WhileStatement(value) {}
function marshallSyntaxKind_BreakStatement(value) {}
function marshallSyntaxKind_ContinueStatement(value) {}
function marshallSyntaxKind_ReturnStatement(value) {}
function marshallSyntaxKind_WithStatement(value) {}
function marshallSyntaxKind_ThrowStatement(value) {}
function marshallSyntaxKind_JSDocTypedefTag(value) {}
function marshallSyntaxKind_JSDocCallbackTag(value) {}
function marshallSyntaxKind_JSDocSignature(value) {}
function marshallSyntaxKind_JSDocParameterTag(value) {}
function marshallSyntaxKind_JSDocReturnTag(value) {}
function marshallSyntaxKind_ImportDeclaration(value) {}
function marshallSyntaxKind_ImportClause(value) {}
function marshallSyntaxKind_NamespaceImport(value) {}
function marshallSyntaxKind_NamedImports(value) {}
function marshallSyntaxKind_ImportSpecifier(value) {}
function marshallSyntaxKind_NamespaceExportDeclaration(value) {}
function marshallSyntaxKind_ExportAssignment(value) {}
function marshallSyntaxKind_JSDocAllType(value) {}
function marshallSyntaxKind_JSDocUnknownType(value) {}
function marshallSyntaxKind_JSDocNonNullableType(value) {}
function marshallSyntaxKind_JSDocNullableType(value) {}
function marshallSyntaxKind_JSDocOptionalType(value) {}
function marshallSyntaxKind_JSDocVariadicType(value) {}
function marshallSyntaxKind_JSDocNamepathType(value) {}
function marshallSyntaxKind_JSDocTag(value) {}
function marshallSyntaxKind_JSDocAuthorTag(value) {}
function marshallSyntaxKind_JSDocClassTag(value) {}
function marshallSyntaxKind_JSDocPublicTag(value) {}
function marshallSyntaxKind_JSDocPrivateTag(value) {}
function marshallSyntaxKind_JSDocProtectedTag(value) {}
function marshallSyntaxKind_JSDocReadonlyTag(value) {}
function marshallSyntaxKind_JSDocEnumTag(value) {}
function marshallSyntaxKind_JSDocThisTag(value) {}
function marshallSyntaxKind_JSDocTypeTag(value) {}
function marshallSyntaxKind_JSDocPropertyTag(value) {}
function marshallNodeArray(value) {}
function marshallNode(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_AbstractKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AbstractKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_AsyncKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AsyncKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ConstKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ConstKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_DeclareKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DeclareKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_DefaultKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DefaultKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ExportKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExportKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_PublicKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PublicKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_PrivateKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PrivateKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ProtectedKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ProtectedKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ReadonlyKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ReadonlyKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_StaticKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_StaticKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_QuestionDotToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_QuestionDotToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_QuestionToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_QuestionToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_EqualsGreaterThanToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EqualsGreaterThanToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_AsteriskToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AsteriskToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ExclamationToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExclamationToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_AwaitKeyword(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AwaitKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_EndOfFileToken_old(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EndOfFileToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_EndOfFileToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EndOfFileToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallToken_EqualsToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EqualsToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_ColonToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ColonToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallToken_DotDotDotToken(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DotDotDotToken(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallModifiersArray(value) {}
function marshallModifierToken(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallDecorator(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Decorator(value.kind);
  marshallNamedDeclaration(value.parent);
  marshallLeftHandSideExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedDeclaration(value) {
  assert(value instanceof NodeObject);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallDeclarationName(value.name);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallIdentifier(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Identifier(value.kind);
  marshallPath(value.escapedText);
  const exists_originalKeywordKind = hop(value, "originalKeywordKind");
  if (exists_originalKeywordKind) {
    marshallNumberType(value.originalKeywordKind);
  }
  const exists_autoGenerateFlags = hop(value, "autoGenerateFlags");
  if (exists_autoGenerateFlags) {
    marshallNumberType(value.autoGenerateFlags);
  }
  const exists_autoGenerateId = hop(value, "autoGenerateId");
  if (exists_autoGenerateId) {
    marshallNumberType(value.autoGenerateId);
  }
  const exists_isInJSDocNamespace = hop(value, "isInJSDocNamespace");
  if (exists_isInJSDocNamespace) {
    marshallBooleanType(value.isInJSDocNamespace);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsdocDotPos = hop(value, "jsdocDotPos");
  if (exists_jsdocDotPos) {
    marshallNumberType(value.jsdocDotPos);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallStringLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_StringLiteral(value.kind);
  const exists_textSourceNode = hop(value, "textSourceNode");
  if (exists_textSourceNode) {
    marshallStringLiteral_TextSourceNode(value.textSourceNode);
  }
  const exists_singleQuote = hop(value, "singleQuote");
  if (exists_singleQuote) {
    marshallBooleanType(value.singleQuote);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNumericLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NumericLiteral(value.kind);
  marshallNumberType(value.numericLiteralFlags);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNoSubstitutionTemplateLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NoSubstitutionTemplateLiteral(value.kind);
  const exists_templateFlags = hop(value, "templateFlags");
  if (exists_templateFlags) {
    marshallNumberType(value.templateFlags);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_rawText = hop(value, "rawText");
  if (exists_rawText) {
    marshallPath(value.rawText);
  }
}
function marshallStringLiteral_TextSourceNode(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallComputedPropertyName(value) {
  assert(value instanceof NodeObject);
  marshallDeclaration(value.parent);
  marshallSyntaxKind_ComputedPropertyName(value.kind);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPrivateIdentifier(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PrivateIdentifier(value.kind);
  marshallPath(value.escapedText);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallElementAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ElementAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallExpression(value.argumentExpression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLeftHandSideExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallObjectBindingPattern(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ObjectBindingPattern(value.kind);
  marshallObjectBindingPattern_Parent(value.parent);
  marshallNodeArray(value.elements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallParameterDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Parameter(value.kind);
  marshallSignatureDeclaration(value.parent);
  const exists_dotDotDotToken = hop(value, "dotDotDotToken");
  if (exists_dotDotDotToken) {
    marshallToken_DotDotDotToken(value.dotDotDotToken);
  }
  marshallBindingName(value.name);
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallCallSignatureDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CallSignature(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
}
function marshallPropertyName(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTypeNode(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDoc(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocComment(value.kind);
  marshallHasJSDoc(value.parent);
  const exists_tags = hop(value, "tags");
  if (exists_tags) {
    marshallNodeArray(value.tags);
  }
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallConstructSignatureDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ConstructSignature(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
}
function marshallArray_JSDoc(value) {}
function marshallArray_JSDocTag(value) {}
function marshallJSDocTag(value) {
  assert(value instanceof NodeObject);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocTypeLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocTypeLiteral(value.kind);
  const exists_jsDocPropertyTags = hop(value, "jsDocPropertyTags");
  if (exists_jsDocPropertyTags) {
    marshallArray_JSDocPropertyLikeTag(value.jsDocPropertyTags);
  }
  const exists_isArrayType = hop(value, "isArrayType");
  if (exists_isArrayType) {
    marshallBooleanType(value.isArrayType);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallArray_JSDocPropertyLikeTag(value) {}
function marshallJSDocPropertyLikeTag(value) {
  assert(value instanceof NodeObject);
  marshallJSDoc(value.parent);
  marshallEntityName(value.name);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallBooleanType(value.isNameFirst);
  marshallBooleanType(value.isBracketed);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallQualifiedName(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_QualifiedName(value.kind);
  marshallEntityName(value.left);
  marshallIdentifier(value.right);
  const exists_jsdocDotPos = hop(value, "jsdocDotPos");
  if (exists_jsdocDotPos) {
    marshallNumberType(value.jsdocDotPos);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallEntityName(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocTypeExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocTypeExpression(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallMethodSignature(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_MethodSignature(value.kind);
  marshallObjectTypeDeclaration(value.parent);
  marshallPropertyName(value.name);
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
}
function marshallClassDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ClassDeclaration(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  const exists_heritageClauses = hop(value, "heritageClauses");
  if (exists_heritageClauses) {
    marshallNodeArray(value.heritageClauses);
  }
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallClassExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ClassExpression(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  const exists_heritageClauses = hop(value, "heritageClauses");
  if (exists_heritageClauses) {
    marshallNodeArray(value.heritageClauses);
  }
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallInterfaceDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_InterfaceDeclaration(value.kind);
  marshallIdentifier(value.name);
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  const exists_heritageClauses = hop(value, "heritageClauses");
  if (exists_heritageClauses) {
    marshallNodeArray(value.heritageClauses);
  }
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallTypeLiteralNode(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TypeLiteral(value.kind);
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallObjectTypeDeclaration(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPropertySignature(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PropertySignature(value.kind);
  marshallPropertyName(value.name);
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallArrowFunction(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ArrowFunction(value.kind);
  marshallToken_EqualsGreaterThanToken(value.equalsGreaterThanToken);
  marshallConciseBody(value.body);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallBlock(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Block(value.kind);
  marshallNodeArray(value.statements);
  const exists_multiLine = hop(value, "multiLine");
  if (exists_multiLine) {
    marshallBooleanType(value.multiLine);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallConciseBody(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallFlowNode(value) {
  assert(value instanceof NodeObject);
}
function marshallParenthesizedExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ParenthesizedExpression(value.kind);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallSpreadAssignment(value) {
  assert(value instanceof NodeObject);
  marshallObjectLiteralExpression(value.parent);
  marshallSyntaxKind_SpreadAssignment(value.kind);
  marshallExpression(value.expression);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallObjectLiteralExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ObjectLiteralExpression(value.kind);
  const exists_multiLine = hop(value, "multiLine");
  if (exists_multiLine) {
    marshallBooleanType(value.multiLine);
  }
  marshallNodeArray(value.properties);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallShorthandPropertyAssignment(value) {
  assert(value instanceof NodeObject);
  marshallObjectLiteralExpression(value.parent);
  marshallSyntaxKind_ShorthandPropertyAssignment(value.kind);
  marshallIdentifier(value.name);
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_equalsToken = hop(value, "equalsToken");
  if (exists_equalsToken) {
    marshallToken_EqualsToken(value.equalsToken);
  }
  const exists_objectAssignmentInitializer = hop(
    value,
    "objectAssignmentInitializer"
  );
  if (exists_objectAssignmentInitializer) {
    marshallExpression(value.objectAssignmentInitializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallPropertyAssignment(value) {
  assert(value instanceof NodeObject);
  marshallObjectLiteralExpression(value.parent);
  marshallSyntaxKind_PropertyAssignment(value.kind);
  marshallPropertyName(value.name);
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  marshallExpression(value.initializer);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallFunctionExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_FunctionExpression(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  marshallBlock(value.body);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallLabeledStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_LabeledStatement(value.kind);
  marshallIdentifier(value.label);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallStatement(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallExpressionStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExpressionStatement(value.kind);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallVariableStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_VariableStatement(value.kind);
  marshallVariableDeclarationList(value.declarationList);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallVariableDeclarationList(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_VariableDeclarationList(value.kind);
  marshallType164(value.parent);
  marshallNodeArray(value.declarations);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallForStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ForStatement(value.kind);
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallForInitializer(value.initializer);
  }
  const exists_condition = hop(value, "condition");
  if (exists_condition) {
    marshallExpression(value.condition);
  }
  const exists_incrementor = hop(value, "incrementor");
  if (exists_incrementor) {
    marshallExpression(value.incrementor);
  }
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallForInitializer(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallForInStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ForInStatement(value.kind);
  marshallForInitializer(value.initializer);
  marshallExpression(value.expression);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallForOfStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ForOfStatement(value.kind);
  const exists_awaitModifier = hop(value, "awaitModifier");
  if (exists_awaitModifier) {
    marshallToken_AwaitKeyword(value.awaitModifier);
  }
  marshallForInitializer(value.initializer);
  marshallExpression(value.expression);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType164(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallFunctionDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_FunctionDeclaration(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallBlock(value.body);
  }
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallConstructorDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Constructor(value.kind);
  marshallClassLikeDeclaration(value.parent);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallBlock(value.body);
  }
  const exists_returnFlowNode = hop(value, "returnFlowNode");
  if (exists_returnFlowNode) {
    marshallFlowNode(value.returnFlowNode);
  }
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallClassLikeDeclaration(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallMethodDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_MethodDeclaration(value.kind);
  marshallType178(value.parent);
  marshallPropertyName(value.name);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallBlock(value.body);
  }
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType178(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPropertyDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PropertyDeclaration(value.kind);
  marshallClassLikeDeclaration(value.parent);
  marshallPropertyName(value.name);
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallGetAccessorDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_GetAccessor(value.kind);
  marshallType187(value.parent);
  marshallPropertyName(value.name);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallBlock(value.body);
  }
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType187(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallSetAccessorDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SetAccessor(value.kind);
  marshallType192(value.parent);
  marshallPropertyName(value.name);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallBlock(value.body);
  }
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_endFlowNode = hop(value, "endFlowNode");
  if (exists_endFlowNode) {
    marshallFlowNode(value.endFlowNode);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType192(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTypeAliasDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TypeAliasDeclaration(value.kind);
  marshallIdentifier(value.name);
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallEnumMember(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EnumMember(value.kind);
  marshallEnumDeclaration(value.parent);
  marshallPropertyName(value.name);
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallEnumDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EnumDeclaration(value.kind);
  marshallIdentifier(value.name);
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallModuleDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ModuleDeclaration(value.kind);
  marshallPath(value.parent);
  marshallModuleName(value.name);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallModuleDeclaration_Body(value.body);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallSourceFile(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SourceFile(value.kind);
  marshallNodeArray(value.statements);
  marshallToken_EndOfFileToken_old(value.endOfFileToken);
  marshallPath(value.fileName);
  marshallPath(value.path);
  marshallPath(value.text);
  marshallPath(value.resolvedPath);
  marshallPath(value.originalFileName);
  const exists_redirectInfo = hop(value, "redirectInfo");
  if (exists_redirectInfo) {
    marshallRedirectInfo(value.redirectInfo);
  }
  marshallArray_AmdDependency(value.amdDependencies);
  const exists_moduleName = hop(value, "moduleName");
  if (exists_moduleName) {
    marshallPath(value.moduleName);
  }
  marshallArray_FileReference(value.referencedFiles);
  marshallArray_FileReference(value.typeReferenceDirectives);
  marshallArray_FileReference(value.libReferenceDirectives);
  marshallNumberType(value.languageVariant);
  marshallBooleanType(value.isDeclarationFile);
  const exists_renamedDependencies = hop(value, "renamedDependencies");
  if (exists_renamedDependencies) {
    marshallReadonlyMap_string_(value.renamedDependencies);
  }
  marshallBooleanType(value.hasNoDefaultLib);
  marshallNumberType(value.languageVersion);
  marshallNumberType(value.scriptKind);
  const exists_externalModuleIndicator = hop(value, "externalModuleIndicator");
  if (exists_externalModuleIndicator) {
    marshallNode(value.externalModuleIndicator);
  }
  const exists_commonJsModuleIndicator = hop(value, "commonJsModuleIndicator");
  if (exists_commonJsModuleIndicator) {
    marshallNode(value.commonJsModuleIndicator);
  }
  const exists_jsGlobalAugmentations = hop(value, "jsGlobalAugmentations");
  if (exists_jsGlobalAugmentations) {
    marshallSymbolTable(value.jsGlobalAugmentations);
  }
  marshallMap(value.identifiers);
  marshallNumberType(value.nodeCount);
  marshallNumberType(value.identifierCount);
  marshallNumberType(value.symbolCount);
  marshallArray_DiagnosticWithLocation(value.parseDiagnostics);
  marshallArray_DiagnosticWithLocation(value.bindDiagnostics);
  const exists_bindSuggestionDiagnostics = hop(
    value,
    "bindSuggestionDiagnostics"
  );
  if (exists_bindSuggestionDiagnostics) {
    marshallArray_DiagnosticWithLocation(value.bindSuggestionDiagnostics);
  }
  const exists_jsDocDiagnostics = hop(value, "jsDocDiagnostics");
  if (exists_jsDocDiagnostics) {
    marshallArray_DiagnosticWithLocation(value.jsDocDiagnostics);
  }
  const exists_additionalSyntacticDiagnostics = hop(
    value,
    "additionalSyntacticDiagnostics"
  );
  if (exists_additionalSyntacticDiagnostics) {
    marshallArray_DiagnosticWithLocation(value.additionalSyntacticDiagnostics);
  }
  marshallArray_Number(value.lineMap);
  const exists_checkJsDirective = hop(value, "checkJsDirective");
  if (exists_checkJsDirective) {
    marshallCheckJsDirective(value.checkJsDirective);
  }
  marshallPath(value.version);
  marshallReadonlyPragmaMap(value.pragmas);
  const exists_localJsxNamespace = hop(value, "localJsxNamespace");
  if (exists_localJsxNamespace) {
    marshallPath(value.localJsxNamespace);
  }
  const exists_localJsxFactory = hop(value, "localJsxFactory");
  if (exists_localJsxFactory) {
    marshallEntityName(value.localJsxFactory);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPath(value) {}
function marshallRedirectInfo(value) {
  assert(value instanceof NodeObject);
  marshallSourceFile(value.redirectTarget);
  marshallSourceFile(value.unredirected);
}
function marshallArray_AmdDependency(value) {}
function marshallAmdDependency(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.path);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPath(value.name);
  }
}
function marshallArray_FileReference(value) {}
function marshallFileReference(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.fileName);
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallReadonlyMap_string_(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.size);
}
function marshallSymbolTable(value) {
  assert(value instanceof NodeObject);
}
function marshallMap(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.size);
}
function marshallDiagnosticWithLocation(value) {
  assert(value instanceof NodeObject);
  marshallSourceFile(value.file);
  marshallNumberType(value.start);
  marshallNumberType(value.length);
  const exists_reportsUnnecessary = hop(value, "reportsUnnecessary");
  if (exists_reportsUnnecessary) {
    marshallType227(value.reportsUnnecessary);
  }
  const exists_source = hop(value, "source");
  if (exists_source) {
    marshallPath(value.source);
  }
  const exists_relatedInformation = hop(value, "relatedInformation");
  if (exists_relatedInformation) {
    marshallArray_DiagnosticRelatedInformation(value.relatedInformation);
  }
  marshallNumberType(value.category);
  marshallNumberType(value.code);
  marshallType233(value.messageText);
}
function marshallType227(value) {
  assert(value instanceof NodeObject);
}
function marshallArray_DiagnosticRelatedInformation(value) {}
function marshallDiagnosticRelatedInformation(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.category);
  marshallNumberType(value.code);
  const exists_file = hop(value, "file");
  if (exists_file) {
    marshallSourceFile(value.file);
  }
  const exists_start = hop(value, "start");
  if (exists_start) {
    marshallNumberType(value.start);
  }
  const exists_length = hop(value, "length");
  if (exists_length) {
    marshallNumberType(value.length);
  }
  marshallType232(value.messageText);
}
function marshallDiagnosticMessageChain(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.messageText);
  marshallNumberType(value.category);
  marshallNumberType(value.code);
  const exists_next = hop(value, "next");
  if (exists_next) {
    marshallArray_DiagnosticMessageChain(value.next);
  }
}
function marshallArray_DiagnosticMessageChain(value) {}
function marshallType232(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType233(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallArray_DiagnosticWithLocation(value) {}
function marshallArray_Number(value) {}
function marshallCheckJsDirective(value) {
  assert(value instanceof NodeObject);
  marshallBooleanType(value.enabled);
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallReadonlyPragmaMap(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.size);
}
function marshallModuleBlock(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ModuleBlock(value.kind);
  marshallModuleDeclaration(value.parent);
  marshallNodeArray(value.statements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamespaceBody(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallNamespaceDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallIdentifier(value.name);
  marshallNamespaceBody(value.body);
  marshallSyntaxKind_ModuleDeclaration(value.kind);
  marshallPath(value.parent);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallModuleName(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallModuleDeclaration_Body(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallImportEqualsDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportEqualsDeclaration(value.kind);
  marshallType260(value.parent);
  marshallIdentifier(value.name);
  marshallModuleReference(value.moduleReference);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType260(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallExternalModuleReference(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExternalModuleReference(value.kind);
  marshallImportEqualsDeclaration(value.parent);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallModuleReference(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallIndexSignatureDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_IndexSignature(value.kind);
  marshallObjectTypeDeclaration(value.parent);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
}
function marshallFunctionTypeNode(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_FunctionType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallConstructorTypeNode(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ConstructorType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallJSDocFunctionType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocFunctionType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  marshallNodeArray(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallExportDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExportDeclaration(value.kind);
  marshallType284(value.parent);
  marshallBooleanType(value.isTypeOnly);
  const exists_exportClause = hop(value, "exportClause");
  if (exists_exportClause) {
    marshallNamedExportBindings(value.exportClause);
  }
  const exists_moduleSpecifier = hop(value, "moduleSpecifier");
  if (exists_moduleSpecifier) {
    marshallExpression(value.moduleSpecifier);
  }
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallType290(value.name);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType284(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallNamespaceExport(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NamespaceExport(value.kind);
  marshallExportDeclaration(value.parent);
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedExports(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NamedExports(value.kind);
  marshallExportDeclaration(value.parent);
  marshallNodeArray(value.elements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedExportBindings(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType290(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocContainer(value) {
  assert(value instanceof NodeObject);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallHasJSDoc(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallSignatureDeclaration(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallArrayBindingPattern(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ArrayBindingPattern(value.kind);
  marshallType315(value.parent);
  marshallNodeArray(value.elements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallVariableDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_VariableDeclaration(value.kind);
  marshallType311(value.parent);
  marshallBindingName(value.name);
  const exists_exclamationToken = hop(value, "exclamationToken");
  if (exists_exclamationToken) {
    marshallToken_ExclamationToken(value.exclamationToken);
  }
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallTypeNode(value.type);
  }
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCatchClause(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CatchClause(value.kind);
  marshallTryStatement(value.parent);
  const exists_variableDeclaration = hop(value, "variableDeclaration");
  if (exists_variableDeclaration) {
    marshallVariableDeclaration(value.variableDeclaration);
  }
  marshallBlock(value.block);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTryStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TryStatement(value.kind);
  marshallBlock(value.tryBlock);
  const exists_catchClause = hop(value, "catchClause");
  if (exists_catchClause) {
    marshallCatchClause(value.catchClause);
  }
  const exists_finallyBlock = hop(value, "finallyBlock");
  if (exists_finallyBlock) {
    marshallBlock(value.finallyBlock);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType311(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_BindingElement(value.kind);
  marshallBindingPattern(value.parent);
  const exists_propertyName = hop(value, "propertyName");
  if (exists_propertyName) {
    marshallPropertyName(value.propertyName);
  }
  const exists_dotDotDotToken = hop(value, "dotDotDotToken");
  if (exists_dotDotDotToken) {
    marshallToken_DotDotDotToken(value.dotDotDotToken);
  }
  marshallBindingName(value.name);
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallExpression(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBindingPattern(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType315(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingName(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallObjectBindingPattern_Parent(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPropertyAccessEntityNameExpression(value) {
  assert(value instanceof NodeObject);
  marshallEntityNameExpression(value.expression);
  marshallIdentifier(value.name);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallEntityNameExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallDeclarationName(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTypeParameterDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TypeParameter(value.kind);
  marshallTypeParameterDeclaration_Parent(value.parent);
  marshallIdentifier(value.name);
  const exists_constraint = hop(value, "constraint");
  if (exists_constraint) {
    marshallTypeNode(value.constraint);
  }
  const exists_default = hop(value, "default");
  if (exists_default) {
    marshallTypeNode(value.default);
  }
  const exists_expression = hop(value, "expression");
  if (exists_expression) {
    marshallExpression(value.expression);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallInferTypeNode(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_InferType(value.kind);
  marshallTypeParameterDeclaration(value.typeParameter);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocTemplateTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocTemplateTag(value.kind);
  const exists_constraint = hop(value, "constraint");
  if (exists_constraint) {
    marshallJSDocTypeExpression(value.constraint);
  }
  marshallNodeArray(value.typeParameters);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocOrJSDocTypeLiteral(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTypeParameterDeclaration_Parent(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallHeritageClause(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_HeritageClause(value.kind);
  marshallHeritageClause_Parent(value.parent);
  marshallNumberType(value.token);
  marshallNodeArray(value.types);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallHeritageClause_Parent(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallClassElement(value) {
  assert(value instanceof NodeObject);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTypeElement(value) {
  assert(value instanceof NodeObject);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  const exists_questionToken = hop(value, "questionToken");
  if (exists_questionToken) {
    marshallToken_QuestionToken(value.questionToken);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallExpressionWithTypeArguments(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExpressionWithTypeArguments(value.kind);
  marshallExpressionWithTypeArguments_Parent(value.parent);
  marshallLeftHandSideExpression(value.expression);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocAugmentsTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocAugmentsTag(value.kind);
  marshallJSDocAugmentsTag_Class(value.class);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocAugmentsTag_Class_Expression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocAugmentsTag_Class(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExpressionWithTypeArguments(value.kind);
  marshallExpressionWithTypeArguments_Parent(value.parent);
  marshallJSDocAugmentsTag_Class_Expression(value.expression);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallExpressionWithTypeArguments_Parent(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallOmittedExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_OmittedExpression(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallExportSpecifier(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExportSpecifier(value.kind);
  marshallNamedExports(value.parent);
  const exists_propertyName = hop(value, "propertyName");
  if (exists_propertyName) {
    marshallIdentifier(value.propertyName);
  }
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallStringLiteralLike(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPartiallyEmittedExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PartiallyEmittedExpression(value.kind);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallUnaryExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallUpdateExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPrefixUnaryExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PrefixUnaryExpression(value.kind);
  marshallNumberType(value.operator);
  marshallUnaryExpression(value.operand);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPostfixUnaryExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PostfixUnaryExpression(value.kind);
  marshallLeftHandSideExpression(value.operand);
  marshallNumberType(value.operator);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallMemberExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPrimaryExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNullLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NullKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBooleanLiteral(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallThisExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ThisKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSuperExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SuperKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallImportExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportKeyword(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDeleteExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DeleteExpression(value.kind);
  marshallUnaryExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTypeOfExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TypeOfExpression(value.kind);
  marshallUnaryExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallVoidExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_VoidExpression(value.kind);
  marshallUnaryExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallAwaitExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AwaitExpression(value.kind);
  marshallUnaryExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallYieldExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_YieldExpression(value.kind);
  const exists_asteriskToken = hop(value, "asteriskToken");
  if (exists_asteriskToken) {
    marshallToken_AsteriskToken(value.asteriskToken);
  }
  const exists_expression = hop(value, "expression");
  if (exists_expression) {
    marshallExpression(value.expression);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSyntheticExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SyntheticExpression(value.kind);
  marshallBooleanType(value.isSpread);
  marshall__type(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshall__type(value) {
  assert(value instanceof NodeObject);
}
function marshallOperatorToken(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBinaryExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_BinaryExpression(value.kind);
  marshallExpression(value.left);
  marshallOperatorToken(value.operatorToken);
  marshallExpression(value.right);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallAssignmentOperatorToken(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallArrayLiteralExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ArrayLiteralExpression(value.kind);
  marshallNodeArray(value.elements);
  const exists_multiLine = hop(value, "multiLine");
  if (exists_multiLine) {
    marshallBooleanType(value.multiLine);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDestructuringAssignment(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPropertyAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallIdentifierOrPrivateIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallIdentifierOrPrivateIdentifier(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallSpreadElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SpreadElement(value.kind);
  marshallType402(value.parent);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCallExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CallExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNodeArray(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNewExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NewExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_arguments = hop(value, "arguments");
  if (exists_arguments) {
    marshallNodeArray(value.arguments);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType402(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingOrAssignmentElement(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingOrAssignmentElementRestIndicator(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingOrAssignmentElementTarget(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallObjectBindingOrAssignmentPattern(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallArrayBindingOrAssignmentPattern(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallAssignmentPattern(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindingOrAssignmentPattern(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallConditionalExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ConditionalExpression(value.kind);
  marshallExpression(value.condition);
  marshallToken_QuestionToken(value.questionToken);
  marshallExpression(value.whenTrue);
  marshallToken_ColonToken(value.colonToken);
  marshallExpression(value.whenFalse);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralLikeNode(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateLiteralLikeNode(value) {
  assert(value instanceof NodeObject);
  const exists_rawText = hop(value, "rawText");
  if (exists_rawText) {
    marshallPath(value.rawText);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralExpression(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallRegularExpressionLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_RegularExpressionLiteral(value.kind);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBigIntLiteral(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_BigIntLiteral(value.kind);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateHead(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TemplateHead(value.kind);
  marshallTemplateExpression(value.parent);
  const exists_templateFlags = hop(value, "templateFlags");
  if (exists_templateFlags) {
    marshallNumberType(value.templateFlags);
  }
  const exists_rawText = hop(value, "rawText");
  if (exists_rawText) {
    marshallPath(value.rawText);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TemplateExpression(value.kind);
  marshallTemplateHead(value.head);
  marshallNodeArray(value.templateSpans);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateSpan(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TemplateSpan(value.kind);
  marshallTemplateExpression(value.parent);
  marshallExpression(value.expression);
  marshallType432(value.literal);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateMiddle(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TemplateMiddle(value.kind);
  marshallTemplateSpan(value.parent);
  const exists_templateFlags = hop(value, "templateFlags");
  if (exists_templateFlags) {
    marshallNumberType(value.templateFlags);
  }
  const exists_rawText = hop(value, "rawText");
  if (exists_rawText) {
    marshallPath(value.rawText);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTemplateTail(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TemplateTail(value.kind);
  marshallTemplateSpan(value.parent);
  const exists_templateFlags = hop(value, "templateFlags");
  if (exists_templateFlags) {
    marshallNumberType(value.templateFlags);
  }
  const exists_rawText = hop(value, "rawText");
  if (exists_rawText) {
    marshallPath(value.rawText);
  }
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType432(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTemplateLiteral(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallEntityNameOrEntityNameExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallAccessExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPrivateIdentifierPropertyAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallPrivateIdentifier(value.name);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPropertyAccessChain(value) {
  assert(value instanceof NodeObject);
  marshallIdentifier(value.name);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallPropertyAccessChainRoot(value) {
  assert(value instanceof NodeObject);
  marshallToken_QuestionDotToken(value.questionDotToken);
  marshallIdentifier(value.name);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSuperPropertyAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallSuperExpression(value.expression);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallIdentifierOrPrivateIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallElementAccessChain(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ElementAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallExpression(value.argumentExpression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallElementAccessChainRoot(value) {
  assert(value instanceof NodeObject);
  marshallToken_QuestionDotToken(value.questionDotToken);
  marshallSyntaxKind_ElementAccessExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  marshallExpression(value.argumentExpression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSuperElementAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallSuperExpression(value.expression);
  marshallSyntaxKind_ElementAccessExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallExpression(value.argumentExpression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSuperProperty(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallCallChain(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CallExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNodeArray(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCallChainRoot(value) {
  assert(value instanceof NodeObject);
  marshallToken_QuestionDotToken(value.questionDotToken);
  marshallSyntaxKind_CallExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNodeArray(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallOptionalChain(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallOptionalChainRoot(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallWellKnownSymbolExpression(value) {
  assert(value instanceof NodeObject);
  marshallIdentifierWithEscapedTextIsLiteralTypeSymbol(value.expression);
  marshallIdentifier(value.name);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralTypeSymbol(value) {}
function marshallIdentifierWithEscapedTextIsLiteralTypeSymbol(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Identifier(value.kind);
  marshallLiteralTypeSymbol(value.escapedText);
  const exists_originalKeywordKind = hop(value, "originalKeywordKind");
  if (exists_originalKeywordKind) {
    marshallNumberType(value.originalKeywordKind);
  }
  const exists_autoGenerateFlags = hop(value, "autoGenerateFlags");
  if (exists_autoGenerateFlags) {
    marshallNumberType(value.autoGenerateFlags);
  }
  const exists_autoGenerateId = hop(value, "autoGenerateId");
  if (exists_autoGenerateId) {
    marshallNumberType(value.autoGenerateId);
  }
  const exists_isInJSDocNamespace = hop(value, "isInJSDocNamespace");
  if (exists_isInJSDocNamespace) {
    marshallBooleanType(value.isInJSDocNamespace);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsdocDotPos = hop(value, "jsdocDotPos");
  if (exists_jsdocDotPos) {
    marshallNumberType(value.jsdocDotPos);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralLikeElementAccessExpression_ArgumentExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindableStaticElementAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallBindableStaticNameExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallLiteralLikeElementAccessExpression_ArgumentExpression(
    value.argumentExpression
  );
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBindableStaticNameExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType460(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindableObjectDefinePropertyCall_Arguments(value) {
  assert(value instanceof Array);
  marshallBindableStaticNameExpression(value[0]);
  marshallType460(value[1]);
  marshallObjectLiteralExpression(value[2]);
}
function marshallBindableObjectDefinePropertyCall(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CallExpression(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallBindableObjectDefinePropertyCall_Arguments(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralLikeElementAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallLeftHandSideExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallLiteralLikeElementAccessExpression_ArgumentExpression(
    value.argumentExpression
  );
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBindableElementAccessExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ElementAccessExpression(value.kind);
  marshallBindableStaticNameExpression(value.expression);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallExpression(value.argumentExpression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType471(value) {
  assert(value instanceof NodeObject);
  marshallLiteralLikeElementAccessExpression_ArgumentExpression(
    value.argumentExpression
  );
}
function marshallType473(value) {
  assert(value instanceof NodeObject);
  marshallBindableStaticNameExpression(value.expression);
}
function marshallBindableStaticAccessExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBindableAccessExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallSuperCall(value) {
  assert(value instanceof NodeObject);
  marshallSuperExpression(value.expression);
  marshallSyntaxKind_CallExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNodeArray(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallImportCall(value) {
  assert(value instanceof NodeObject);
  marshallImportExpression(value.expression);
  marshallSyntaxKind_CallExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallNodeArray(value.arguments);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTaggedTemplateExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TaggedTemplateExpression(value.kind);
  marshallLeftHandSideExpression(value.tag);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallTemplateLiteral(value.template);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxOpeningElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxOpeningElement(value.kind);
  marshallJsxElement(value.parent);
  marshallJsxTagNameExpression(value.tagName);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallJsxAttributes(value.attributes);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxElement(value.kind);
  marshallJsxOpeningElement(value.openingElement);
  marshallNodeArray(value.children);
  marshallJsxClosingElement(value.closingElement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxClosingElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxClosingElement(value.kind);
  marshallJsxElement(value.parent);
  marshallJsxTagNameExpression(value.tagName);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxTagNamePropertyAccess(value) {
  assert(value instanceof NodeObject);
  marshallJsxTagNameExpression(value.expression);
  marshallSyntaxKind_PropertyAccessExpression(value.kind);
  const exists_questionDotToken = hop(value, "questionDotToken");
  if (exists_questionDotToken) {
    marshallToken_QuestionDotToken(value.questionDotToken);
  }
  marshallIdentifierOrPrivateIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxTagNameExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJsxAttributes(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxAttributes(value.kind);
  marshallJsxOpeningLikeElement(value.parent);
  marshallNodeArray(value.properties);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxSelfClosingElement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxSelfClosingElement(value.kind);
  marshallJsxTagNameExpression(value.tagName);
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  marshallJsxAttributes(value.attributes);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxOpeningLikeElement(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallCallLikeExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJsxAttribute(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxAttribute(value.kind);
  marshallJsxAttributes(value.parent);
  marshallIdentifier(value.name);
  const exists_initializer = hop(value, "initializer");
  if (exists_initializer) {
    marshallType508(value.initializer);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxExpression(value.kind);
  marshallType506(value.parent);
  const exists_dotDotDotToken = hop(value, "dotDotDotToken");
  if (exists_dotDotDotToken) {
    marshallToken_DotDotDotToken(value.dotDotDotToken);
  }
  const exists_expression = hop(value, "expression");
  if (exists_expression) {
    marshallExpression(value.expression);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxSpreadAttribute(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxSpreadAttribute(value.kind);
  marshallJsxAttributes(value.parent);
  marshallExpression(value.expression);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallPropertyName(value.name);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType506(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType508(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJsxFragment(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxFragment(value.kind);
  marshallJsxOpeningFragment(value.openingFragment);
  marshallNodeArray(value.children);
  marshallJsxClosingFragment(value.closingFragment);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxOpeningFragment(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxOpeningFragment(value.kind);
  marshallJsxFragment(value.parent);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxClosingFragment(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxClosingFragment(value.kind);
  marshallJsxFragment(value.parent);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxText(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JsxText(value.kind);
  marshallBooleanType(value.containsOnlyTriviaWhiteSpaces);
  marshallJsxElement(value.parent);
  marshallPath(value.text);
  const exists_isUnterminated = hop(value, "isUnterminated");
  if (exists_isUnterminated) {
    marshallBooleanType(value.isUnterminated);
  }
  const exists_hasExtendedUnicodeEscape = hop(
    value,
    "hasExtendedUnicodeEscape"
  );
  if (exists_hasExtendedUnicodeEscape) {
    marshallBooleanType(value.hasExtendedUnicodeEscape);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallAsExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_AsExpression(value.kind);
  marshallExpression(value.expression);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallTypeAssertion(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_TypeAssertionExpression(value.kind);
  marshallTypeNode(value.type);
  marshallUnaryExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallAssertionExpression(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallNonNullExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NonNullExpression(value.kind);
  marshallExpression(value.expression);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallMetaProperty(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_MetaProperty(value.kind);
  marshallNumberType(value.keywordToken);
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallImportMetaProperty(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportKeyword(value.keywordToken);
  marshallIdentifierWithEscapedTextIsLiteralTypemeta(value.name);
  marshallSyntaxKind_MetaProperty(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallLiteralTypemeta(value) {}
function marshallIdentifierWithEscapedTextIsLiteralTypemeta(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_Identifier(value.kind);
  marshallLiteralTypemeta(value.escapedText);
  const exists_originalKeywordKind = hop(value, "originalKeywordKind");
  if (exists_originalKeywordKind) {
    marshallNumberType(value.originalKeywordKind);
  }
  const exists_autoGenerateFlags = hop(value, "autoGenerateFlags");
  if (exists_autoGenerateFlags) {
    marshallNumberType(value.autoGenerateFlags);
  }
  const exists_autoGenerateId = hop(value, "autoGenerateId");
  if (exists_autoGenerateId) {
    marshallNumberType(value.autoGenerateId);
  }
  const exists_isInJSDocNamespace = hop(value, "isInJSDocNamespace");
  if (exists_isInJSDocNamespace) {
    marshallBooleanType(value.isInJSDocNamespace);
  }
  const exists_typeArguments = hop(value, "typeArguments");
  if (exists_typeArguments) {
    marshallNodeArray(value.typeArguments);
  }
  const exists_jsdocDotPos = hop(value, "jsdocDotPos");
  if (exists_jsdocDotPos) {
    marshallNumberType(value.jsdocDotPos);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJsxAttributeLike(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJsxChild(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallNotEmittedStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NotEmittedStatement(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallEndOfDeclarationMarker(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EndOfDeclarationMarker(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCommaListExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CommaListExpression(value.kind);
  marshallNodeArray(value.elements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallMergeDeclarationMarker(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_MergeDeclarationMarker(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSyntheticReferenceExpression(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SyntheticReferenceExpression(value.kind);
  marshallExpression(value.expression);
  marshallExpression(value.thisArg);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallEmptyStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_EmptyStatement(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDebuggerStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DebuggerStatement(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallMissingDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_MissingDeclaration(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCaseClause(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CaseClause(value.kind);
  marshallCaseBlock(value.parent);
  marshallExpression(value.expression);
  marshallNodeArray(value.statements);
  const exists_fallthroughFlowNode = hop(value, "fallthroughFlowNode");
  if (exists_fallthroughFlowNode) {
    marshallFlowNode(value.fallthroughFlowNode);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCaseBlock(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_CaseBlock(value.kind);
  marshallSwitchStatement(value.parent);
  marshallNodeArray(value.clauses);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSwitchStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_SwitchStatement(value.kind);
  marshallExpression(value.expression);
  marshallCaseBlock(value.caseBlock);
  const exists_possiblyExhaustive = hop(value, "possiblyExhaustive");
  if (exists_possiblyExhaustive) {
    marshallBooleanType(value.possiblyExhaustive);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDefaultClause(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DefaultClause(value.kind);
  marshallCaseBlock(value.parent);
  marshallNodeArray(value.statements);
  const exists_fallthroughFlowNode = hop(value, "fallthroughFlowNode");
  if (exists_fallthroughFlowNode) {
    marshallFlowNode(value.fallthroughFlowNode);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBlockLike(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallPrologueDirective(value) {
  assert(value instanceof NodeObject);
  marshallStringLiteral(value.expression);
  marshallSyntaxKind_ExpressionStatement(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallIfStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_IfStatement(value.kind);
  marshallExpression(value.expression);
  marshallStatement(value.thenStatement);
  const exists_elseStatement = hop(value, "elseStatement");
  if (exists_elseStatement) {
    marshallStatement(value.elseStatement);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallIterationStatement(value) {
  assert(value instanceof NodeObject);
  marshallStatement(value.statement);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDoStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_DoStatement(value.kind);
  marshallExpression(value.expression);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallWhileStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_WhileStatement(value.kind);
  marshallExpression(value.expression);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallForInOrOfStatement(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallBreakStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_BreakStatement(value.kind);
  const exists_label = hop(value, "label");
  if (exists_label) {
    marshallIdentifier(value.label);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallContinueStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ContinueStatement(value.kind);
  const exists_label = hop(value, "label");
  if (exists_label) {
    marshallIdentifier(value.label);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallBreakOrContinueStatement(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallReturnStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ReturnStatement(value.kind);
  const exists_expression = hop(value, "expression");
  if (exists_expression) {
    marshallExpression(value.expression);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallWithStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_WithStatement(value.kind);
  marshallExpression(value.expression);
  marshallStatement(value.statement);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallCaseOrDefaultClause(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallThrowStatement(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ThrowStatement(value.kind);
  const exists_expression = hop(value, "expression");
  if (exists_expression) {
    marshallExpression(value.expression);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocTypedefTag(value) {
  assert(value instanceof NodeObject);
  marshallJSDoc(value.parent);
  marshallSyntaxKind_JSDocTypedefTag(value.kind);
  const exists_fullName = hop(value, "fullName");
  if (exists_fullName) {
    marshallType582(value.fullName);
  }
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallType583(value.typeExpression);
  }
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType582(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallType583(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocCallbackTag(value) {
  assert(value instanceof NodeObject);
  marshallJSDoc(value.parent);
  marshallSyntaxKind_JSDocCallbackTag(value.kind);
  const exists_fullName = hop(value, "fullName");
  if (exists_fullName) {
    marshallType586(value.fullName);
  }
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  marshallJSDocSignature(value.typeExpression);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType586(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocSignature(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocSignature(value.kind);
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallType589(value.typeParameters);
  }
  marshallType590(value.parameters);
  const exists_type = hop(value, "type");
  if (exists_type) {
    marshallJSDocReturnTag(value.type);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType589(value) {}
function marshallType590(value) {}
function marshallJSDocParameterTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocParameterTag(value.kind);
  marshallJSDoc(value.parent);
  marshallEntityName(value.name);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallBooleanType(value.isNameFirst);
  marshallBooleanType(value.isBracketed);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocReturnTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocReturnTag(value.kind);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallDeclarationWithTypeParameters(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallDeclarationWithTypeParameterChildren(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallClassLikeDeclarationBase(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_typeParameters = hop(value, "typeParameters");
  if (exists_typeParameters) {
    marshallNodeArray(value.typeParameters);
  }
  const exists_heritageClauses = hop(value, "heritageClauses");
  if (exists_heritageClauses) {
    marshallNodeArray(value.heritageClauses);
  }
  marshallNodeArray(value.members);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallModuleBody(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallAmbientModuleDeclaration(value) {
  assert(value instanceof NodeObject);
  const exists_body = hop(value, "body");
  if (exists_body) {
    marshallModuleBlock(value.body);
  }
  marshallSyntaxKind_ModuleDeclaration(value.kind);
  marshallType603(value.parent);
  marshallModuleName(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
  const exists_jsDoc = hop(value, "jsDoc");
  if (exists_jsDoc) {
    marshallArray_JSDoc(value.jsDoc);
  }
  const exists_jsDocCache = hop(value, "jsDocCache");
  if (exists_jsDocCache) {
    marshallArray_JSDocTag(value.jsDocCache);
  }
}
function marshallType603(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallImportDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportDeclaration(value.kind);
  marshallType608(value.parent);
  const exists_importClause = hop(value, "importClause");
  if (exists_importClause) {
    marshallImportClause(value.importClause);
  }
  marshallExpression(value.moduleSpecifier);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType608(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallImportClause(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportClause(value.kind);
  marshallImportDeclaration(value.parent);
  marshallBooleanType(value.isTypeOnly);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallIdentifier(value.name);
  }
  const exists_namedBindings = hop(value, "namedBindings");
  if (exists_namedBindings) {
    marshallNamedImportBindings(value.namedBindings);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamespaceImport(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NamespaceImport(value.kind);
  marshallImportClause(value.parent);
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedImports(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NamedImports(value.kind);
  marshallImportClause(value.parent);
  marshallNodeArray(value.elements);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedImportBindings(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallImportSpecifier(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ImportSpecifier(value.kind);
  marshallNamedImports(value.parent);
  const exists_propertyName = hop(value, "propertyName");
  if (exists_propertyName) {
    marshallIdentifier(value.propertyName);
  }
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamespaceExportDeclaration(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_NamespaceExportDeclaration(value.kind);
  marshallIdentifier(value.name);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallNamedImportsOrExports(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallImportOrExportSpecifier(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallTypeOnlyCompatibleAliasDeclaration(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallExportAssignment(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_ExportAssignment(value.kind);
  marshallSourceFile(value.parent);
  const exists_isExportEquals = hop(value, "isExportEquals");
  if (exists_isExportEquals) {
    marshallBooleanType(value.isExportEquals);
  }
  marshallExpression(value.expression);
  const exists_name = hop(value, "name");
  if (exists_name) {
    marshallType625(value.name);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallType625(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallCommentRange(value) {
  assert(value instanceof NodeObject);
  const exists_hasTrailingNewLine = hop(value, "hasTrailingNewLine");
  if (exists_hasTrailingNewLine) {
    marshallBooleanType(value.hasTrailingNewLine);
  }
  marshallNumberType(value.kind);
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallSynthesizedComment(value) {
  assert(value instanceof NodeObject);
  marshallPath(value.text);
  marshallLiteralNegative1(value.pos);
  marshallLiteralNegative1(value.end);
  const exists_hasTrailingNewLine = hop(value, "hasTrailingNewLine");
  if (exists_hasTrailingNewLine) {
    marshallBooleanType(value.hasTrailingNewLine);
  }
  marshallNumberType(value.kind);
}
function marshallLiteralNegative1(value) {}
function marshallJSDocType(value) {
  assert(value instanceof NodeObject);
  marshallNumberType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocAllType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocAllType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocUnknownType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocUnknownType(value.kind);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocNonNullableType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocNonNullableType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocNullableType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocNullableType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocOptionalType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocOptionalType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocVariadicType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocVariadicType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocNamepathType(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocNamepathType(value.kind);
  marshallTypeNode(value.type);
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  marshallNode(value.parent);
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocTypeReferencingNode(value) {
  switch (Object.getPrototypeOf(value)) {
    case NodeObject:
    // TODO    break;
    default:
      throw new Error("unrecognized node in AST");
  }
}
function marshallJSDocUnknownTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocAuthorTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocAuthorTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocClassTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocClassTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocPublicTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocPublicTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocPrivateTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocPrivateTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocProtectedTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocProtectedTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocReadonlyTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocReadonlyTag(value.kind);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocEnumTag(value) {
  assert(value instanceof NodeObject);
  marshallJSDoc(value.parent);
  marshallSyntaxKind_JSDocEnumTag(value.kind);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocThisTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocThisTag(value.kind);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocTypeTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocTypeTag(value.kind);
  marshallJSDocTypeExpression(value.typeExpression);
  marshallJSDocOrJSDocTypeLiteral(value.parent);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
function marshallJSDocPropertyTag(value) {
  assert(value instanceof NodeObject);
  marshallSyntaxKind_JSDocPropertyTag(value.kind);
  marshallJSDoc(value.parent);
  marshallEntityName(value.name);
  const exists_typeExpression = hop(value, "typeExpression");
  if (exists_typeExpression) {
    marshallJSDocTypeExpression(value.typeExpression);
  }
  marshallBooleanType(value.isNameFirst);
  marshallBooleanType(value.isBracketed);
  marshallIdentifier(value.tagName);
  const exists_comment = hop(value, "comment");
  if (exists_comment) {
    marshallPath(value.comment);
  }
  marshallNumberType(value.flags);
  marshallNumberType(value.modifierFlagsCache);
  marshallNumberType(value.transformFlags);
  const exists_decorators = hop(value, "decorators");
  if (exists_decorators) {
    marshallNodeArray(value.decorators);
  }
  const exists_modifiers = hop(value, "modifiers");
  if (exists_modifiers) {
    marshallModifiersArray(value.modifiers);
  }
  const exists_id = hop(value, "id");
  if (exists_id) {
    marshallNumberType(value.id);
  }
  const exists_original = hop(value, "original");
  if (exists_original) {
    marshallNode(value.original);
  }
  marshallNumberType(value.pos);
  marshallNumberType(value.end);
}
